В Expr заменить BUBBLE_UP_IF_NULLREF_OR_ERROR на BUBBLE_UP_IF_ERROR (т.е разрешить none)

Добавить and or xor not в качестве ключевых слов

Доделать Parser::Parse_Pair

Вывод дерева экспрессий

Подумать над тем, не заменить ли self(Ref) на this?

Убрать Eval из ExprEvalList ExprEvalArgsList

Set-метод в Context должен возвращать результат

Проверить ExprIf на ошибки с temp-переменными

ExprEvalList, ExprArgList нет temp-переменных.

Доделать деструкторы

Добавить assert'ы на nullptr в Expr

% остаток - наверное реализовать в виде rem

Лучше добавить assert'ы в Expr Implementation